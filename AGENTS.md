## Go 重写目标

- 项目品牌名称为 `ZRT`，所有面向用户的界面标题、产品名称、文档标题和对外文案必须使用大写 `ZRT`。
- 机器可读标识使用小写 `zrt`，包括 Go module/package、前端包名、二进制、目录、容器镜像、服务名、数据库文件、Redis Key 前缀和 NATS Subject；环境变量使用大写 `ZRT_` 前缀。除原始外部标识外不得使用 `Zrt` 等混合写法。
- 项目源码、文档、配置示例、测试数据和对外文案不得再出现旧项目名称或标识；产品名称统一使用 `ZRT`，机器标识统一使用 `zrt`。
- ZRT 只支持 Linux 和 macOS 的源码构建、本地开发及二进制运行，生产环境优先使用 Linux 容器；不提供、不测试也不维护 Windows 原生构建、运行、Docker npipe、PowerShell、`.exe` 或 `.cmd` 兼容路径，文档和界面不得再把 Windows 列为支持平台。
- 后端使用 Go、Gin 和 GORM 重写，不保留任何 Python 运行时、构建或运维依赖。
- ZRT 使用单进程运行模式；`zrt server` 必须通过 Go Goroutine 同时运行 HTTP/WebSocket、NATS 任务消费、Outbox、监控和调度，不再提供独立 `zrt worker` 命令、进程或部署容器。
- 系统 CPU、内存、Go GC、Worker、任务、消息队列和数据库连接池指标必须在 ZRT 内置页面展示，并受 `monitor.read` 权限保护；不得把 Grafana、Prometheus 或其他外部可观测平台作为查看这些基础指标的前置条件。
- 系统监控的消息队列卡片必须允许拥有 `monitor.manage` 权限的用户清空死信；操作前必须明确二次确认，只清理死信 Stream，不得影响正常任务 Stream，并记录操作审计。死信为空或 JetStream 不可用时不得发起清理。
- 系统设置必须提供运行日志输出配置，至少支持最低日志级别和 HTTP 访问日志开关；保存后必须立即热更新当前进程并持久化，后续重启继续生效。`ZRT_LOG_LEVEL` 只作为尚未保存系统设置时的启动默认值，任何日志配置都不得放宽敏感信息保护。
- 侧栏“日志”页面展示当前 ZRT 系统进程及其各输出通道的系统日志，不采集或混入已部署 Docker 容器的日志。容器日志只在“主机与集群 → 运行资源”的容器只读日志中按需查看。
- `mage start --dev` 是宿主机开发模式，启动前必须停止同一 Compose 项目中遗留的 `api`、`web` 和 Docker-in-Docker 容器，只保留 Redis 与 NATS JetStream；禁止宿主机后端与容器后端同时消费同一 NATS Consumer 或并发访问同一 SQLite 文件。`mage start`、`mage start --dev`、`mage start --docker` 和 Compose 必须统一读取根目录 `.env`；`.env.example` 必须显式列出数据库、Redis、NATS 等运行连接，宿主机使用 `ZRT_DATABASE_*`、`ZRT_REDIS_URL`、`ZRT_NATS_URL`，Compose 因容器路径和网络服务名不同而使用同文件内的 `ZRT_COMPOSE_*` 值映射实际运行变量，禁止把宿主机 `127.0.0.1` 地址直接传给容器。包含后端的启动在 `.env` 缺失、`ZRT_SECRETS_KEY` 为空或格式无效时必须于构建、迁移和启动依赖前失败。不得在代码或 Compose 中硬编码回退密钥、临时生成进程密钥或静默轮换已有密钥。
- 数据库默认使用 SQLite，同时兼容 PostgreSQL 和 MySQL。
- GORM 模型及迁移必须遵守 SQLite、PostgreSQL 和 MySQL 的共同兼容边界；`TEXT`、`BLOB`、`JSON` 等大字段不得配置 `default:''` 等字面量数据库默认值，零值由 Go 显式写入。新增或修改表结构后必须执行三种数据库的真实迁移测试，不能仅凭 SQLite 测试通过判定兼容。
- 系统设置必须提供 SQLite 到 MySQL/PostgreSQL 的受控数据迁移；仅超级管理员可执行，目标库必须为空库且先完成真实连接测试。目标 DSN 不得持久化或记录；迁移完成后必须明确要求切换 `ZRT_DATABASE_DRIVER`/`ZRT_DATABASE_DSN` 并重启，禁止新旧库同时写入。
- 取消通过浏览器 SSH 登录宿主机的功能，不保留宿主机 Web SSH、远程文件管理或交互式服务器终端。
- 保留 Kubernetes Pod 和 Docker 容器内的交互式终端；浏览器通过 WebSocket 连接 Go 后端，由后端使用 Kubernetes Exec 或 Docker Exec API 桥接输入、输出和终端尺寸变更。
- Docker 容器列表必须提供只读控制台日志；运行中容器通过 Docker API 与 WebSocket 持续跟随，重启中和已停止容器仍可读取最近输出。日志查看使用 `cluster.read` 权限，限制可请求的历史行数，在服务端解复用 stdout/stderr，并记录打开和关闭审计；不得通过 SSH 执行任意命令，也不得把容器日志误做成可输入终端。
- 保留 Docker 和 Kubernetes 部署能力，优先通过 Docker API、BuildKit 和 Kubernetes API 实现，不依赖交互式服务器登录。
- 本地 Docker 发布使用当前 ZRT 实例的构建运行时作为内置发布目标；Compose 容器使用隔离的 Docker-in-Docker，二进制和宿主机开发运行直接使用宿主机 Docker CLI/daemon。构建镜像直接在同一 Docker daemon 中校验并部署，必须跳过 SSH 和镜像传输。远程 Docker 发布仍通过独立 SSH 连接传输或拉取镜像。
- Dockerfile 构建方案可以选择镜像仓库，应用和部署方案不得重复绑定镜像仓库。镜像不得借用远程发布目标主机构建；Compose 使用隔离的 Docker-in-Docker，二进制和本地运行使用宿主机 Docker。未绑定镜像仓库时，本地 Docker 环境直接使用构建运行时内的镜像；远程 Docker SSH 环境必须把 `docker save` 镜像流通过 SSH 传给目标主机执行 `docker load`。导出前校验构建 daemon 中的源镜像 ID，加载后读取目标 daemon 的镜像 ID，并用于发布前防替换校验；不同 Docker 版本可能规范化空配置字段，禁止直接要求两端镜像 ID 字符串相等。绑定仓库时由构建运行时 push，目标环境 pull 不可变 Digest。Kubernetes 发布仍必须使用集群可拉取的镜像仓库。
- 远程 Docker 发布环境通过表单配置 SSH 主机地址、端口、用户名，以及密码或私钥；保存前必须实际测试 SSH 登录和 Docker 可用性。发布时通过 SSH 在目标主机执行参数受限的 Docker 操作，不得提供任意宿主机命令或交互式 Shell。本地 Docker 发布不要求或伪造 SSH 配置。
- 内置本地主机的默认展示名称是“本地”，不再使用“ZRT 本机”；该主机不可删除或停用，但允许管理员修改名称并选择当前运行环境真实可用的本地能力。用户关闭某项能力后，迁移和重启不得静默恢复。
- 本地 Docker 能力必须使用官方 Moby 客户端真实连接当前构建运行时后再允许启用：Compose 连接隔离的 Docker-in-Docker，Linux/macOS 本地二进制使用宿主机 Docker。Docker 不可用时界面必须置灰并说明原因，但不得在数据库、Redis 和 NATS 正常时单独使 readiness 失败。
- 本地主机的“直接终端执行”只表示流水线通过固定的非交互式 `sh` 入口执行已保存并随运行快照的部署脚本，不恢复浏览器宿主机终端、临时命令 API 或 PTY。Linux 容器内的 Compose 部署可用，Linux/macOS 本地二进制在存在 `sh` 时可用。
- 保留 WebSocket，用于 Pod/容器交互式终端、实时发布日志、构建日志、任务输出和站内通知；仅删除宿主机 Web SSH 通道。
- HTTP API 和终端 WebSocket 不执行 Origin、Host 或 `Sec-Fetch-Site` 同源限制，允许跨来源接入；仍必须保留登录会话、权限校验、终端子协议校验和操作审计。
- 使用 NATS JetStream 作为消息队列，用于任务投递、持久化、消费确认、失败重投和消费者组；不得再使用 Redis List 充当任务队列。
- 所有 JetStream Consumer 必须配置有限的最大投递次数，禁止无限重试；配置中的 `max_attempts` 包含首次执行，默认最多执行 4 次（首次执行加 3 次重试），耗尽后写入死信主题并将数据库任务标记为失败。
- `ZRT_WORKER_CONCURRENCY` 必须对应真实的固定任务处理协程数，NATS 拉取回调后由有界 Worker 池并行执行；不得把批量拉取数量误当成并发执行数。每个 Docker 构建使用独立检出目录、构建上下文、认证目录和运行标签，不得用全局构建锁把多个 Worker 串行化。
- 多个 Worker 可以并行检出代码和执行 Docker/BuildKit 构建；只有真正更新同一个发布目标的阶段才使用 Redis 分布式锁串行化，不同发布目标仍可并行，禁止用全局发布锁降低吞吐量。
- 重试必须区分可重试错误和永久错误：临时网络故障、超时、限流等可以按退避策略重试；参数错误、权限拒绝、配置缺失、签名失败等必须立即终止，不得消耗后续重试次数。
- 发布与回滚等可能产生外部副作用的任务默认不自动重试，除非对应步骤已实现幂等保护；不得因消息重投而重复发布或重复执行不可逆操作。
- 保留 Redis，用于短期缓存、分布式锁、临时状态、发布并发协调和必要的实时数据；不得将其改为仅进程内实现。
- Git 仓库支持通用 Git，以及 GitLab、Gitea、GitHub、Gitee 的仓库和 Webhook 接入。
- 代码仓库的 Git clone/fetch 凭据与托管平台 API Token 必须分开引用；SSH 私钥只能用于 Git 传输，绝不能作为 HTTP API 凭据发送。平台 API Token 只能引用当前操作者本人所有、平台匹配且类型为 Token 的 Git 凭据；Token 克隆可默认复用同一 Token，显式 API Token 优先。PR/MR API 暂时不可用时不得根据 Commit 猜测源分支或目标分支，也不得阻断同次 Push/Tag 轮询。
- 前端使用 Vue 3、TypeScript、Vite、Vue Router、Pinia 和 Ant Design Vue，并以 Vben Admin 5 `web-antd` 的应用外壳、导航层级、主题令牌、国际化和交互动效为统一基线；旧 React 前端已经删除，不得重新引入 React、Zustand 或 Create React App 运行依赖。
- 旧 Python 后端与安装脚本已经删除；兼容旧数据只能通过 Go 实现的只读迁移工具完成，不得恢复 Python 运行时。

## 重构决策原则

- Go 重构不是对旧 Python 实现的逐行翻译，也不要求无条件保留旧功能和旧交互。
- 实现通用能力前必须先调研 Go 生态中成熟、持续维护且许可证兼容的第三方包，优先复用官方 SDK、事实标准库和经过生产验证的实现，不得在已有可靠方案时无理由重复造轮子；引入前仍须评估维护活跃度、安全记录、依赖体积、可替换性和运维成本，业务规则及安全边界保留在 ZRT 自身代码中。
- Docker Compose、Kubernetes 等运维配置必须用中文注释说明 YAML 锚点、一次性任务、启动依赖、安全限制、健康检查、持久化数据和关键容量或重试参数，不能只给配置而不解释用途与风险。
- 重构过程中发现功能设计不合理、安全风险、可靠性缺陷或运维体验问题时，可以直接调整、替换或删除功能，不限于修复缺陷。
- 决策应以高级运维工程实践为依据，优先保证最小权限、操作审计、幂等执行、并发安全、超时取消、失败重试、灰度发布、健康检查、回滚能力、资源隔离、可观测性和灾难恢复。
- 功能调整必须记录调整原因、行为变化、兼容性影响和数据迁移方式；不得静默改变关键发布语义。
- 涉及删除数据、降低安全边界、不可逆迁移或改变外部系统状态的操作，仍须明确目标并遵守破坏性操作安全规范。

## 流水线与发布模型

- ZRT 面向中小企业的默认发布方式是简单的仓库事件流水线：用户选择需要监听的仓库和分支，配置 `Push`、`Pull Request`、`Tag` 等触发事件，再为命中的事件配置 CI、构建、部署、审批、通知等动作；不得强制用户先建立发布分支、临时环境或复杂的制品晋级模型。
- 分支 `Push`、Pull Request/Merge Request 和 Tag 变化默认由 ZRT 后台按应用检查间隔主动发现，不得要求仓库配置 Webhook 才能触发；分支和 Tag 使用远程 Git 引用，PR/MR 使用托管平台 API 并以公开的 PR Ref 作为构建引用，Webhook 只能作为可选的低延迟通道。每条流水线只有一个代码源，该代码源的事件和具体 Ref 必须使用独立监听游标，允许任意自定义分支或通配规则，不能按环境只保存一个游标，也不能写死 `test` 或 `main`。新应用首次检查只建立基线，后续 Commit 变化、新分支、新 PR/MR 或新 Tag 必须自动创建并推进流水线，不能停留在“发现变更”等待人工再次点击执行。
- 监听规则以“仓库 + 分支匹配条件 + 事件条件 + 动作编排”为基本单位，不得把 `test`、`main` 或固定环境流程写死。例如可配置 `test` 分支变更后自动执行 CI 并发布测试环境，`main` 分支变更后执行对应的生产发布动作。
- `Push` 按发生变更的分支匹配；Pull Request 必须允许按目标分支、来源分支和 opened、updated、merged 等动作细分。代码托管平台在 PR 合并时通常还会发送 Push，事件归一化层和流水线运行必须做幂等去重，禁止同一次合并重复发布。
- Git Tag 本身不天然属于某个分支；Tag 触发规则必须记录并使用标签名、目标 Commit 和可选的标签匹配表达式，不得仅凭“创建 Tag 时所在的本地分支”判断归属。
- 已启用流水线不得因编辑器自动保存而被静默停用；停用并保存为草稿必须是用户明确操作。当前数据模型没有独立草稿版本时，活跃流水线的修改只能通过明确的保存或重新启用操作提交。
- 一个应用可以拥有多条相互独立的流水线；每条流水线分别保存名称、启用状态、代码触发规则、阶段任务和可选的流水线方案绑定，应用本身不得保存唯一的流水线方案或唯一流水线字段。流水线运行、仓库监听游标、事件去重、手动执行和发布计划执行都必须明确绑定 `workflow_id`，不得按应用任取第一条流水线。应用关联的某条流水线绑定方案后必须跟随方案的最新已启用版本，方案保存为草稿时不得影响已绑定流水线；直接编辑该流水线表示仅将这一条转为自定义配置并解除其方案关联，不得影响同一应用的其他流水线。已经创建的流水线运行继续使用启动时快照，不得被方案更新篡改。
- 公共流水线方案是跨应用复用的通用编排，不得绑定、保存或展示具体代码仓库字段；代码源节点只配置启动事件和版本匹配规则。应用使用方案时必须采用该应用自己的代码仓库和凭据，方案更新不得改写应用的仓库关联。具体仓库只在应用配置和运行解析上下文展示，流水线方案及应用流水线编辑器的代码源属性面板均不得展示具体仓库字段；公共方案画布中的代码源副标题统一使用“应用入口”，属性面板不得重复展示仓库归属说明告警。
- 应用列表、应用详情和应用流水线编辑器必须一致展示该应用的全部流水线及每条流水线当前绑定的方案；应用概览必须逐条展示流水线名称、启用状态、触发规则、任务构成和最近运行状态，并按运行与发布记录关联展示当前已知的 Docker 容器、Compose 服务或 Kubernetes 工作负载、发布位置、镜像和部署状态；缺少 `deployment.read` 权限时必须明确显示无权限，禁止伪装成尚未部署。编辑器必须先明确当前流水线，并提供创建、切换、重命名、停用和删除单条流水线的入口。切换某条流水线的方案时必须持久化该流水线绑定并重新加载对应快照，不得覆盖应用内其他流水线，也不得只跳转到公共方案页面造成“已选择但未绑定”的假象。
- 应用概览卡片默认只展示应用、当前运行、当前任务、代码状态和常用操作；关联流水线及部署实例必须按需折叠展开。Docker 部署实例展开后必须复用“运行资源”的只读日志和容器终端能力：日志受 `cluster.read` 保护，终端受 `terminal.open` 保护且只在实时确认容器处于运行状态时可打开；不得把部署记录中的历史成功状态当作容器当前运行状态。
- 新建应用流水线必须先选择模板，不得点击后直接生成一条含糊的默认流水线。内置模板至少提供空白、Go、Node.js 和 Python，并由服务端原子生成“代码源 + 语言测试 + 构建制品 + 部署”的可编辑草稿；模板只保存版本明确的通用运行镜像和测试命令，不得预先绑定具体构建方案、部署方案、环境或主机，用户创建后再补充实际资源配置。
- 手动发布是唯一代码源中的启动选项，不是独立任务或上游阶段；代码源可以同时监听 Push、Pull Request、Tag 并允许人工选择版本启动，二者复用完全相同的后续审核、放行和部署任务。手动执行选择分支或 Tag 后必须由服务端再次固定并校验 Commit，再从代码源直接推进。旧独立手动发布节点及其执行兜底不再保留。
- 发布计划和发布组用于可选的人工批量发布及多应用编排，不能成为仓库事件自动发布的前置条件；简单监听规则应当可以独立完成从事件到动作的完整流水线。
- 发布计划中所有应用均已启用且至少有一条已启用流水线的代码触发节点开启手动发布时必须显示“执行计划”入口；一次执行必须为计划内每个应用明确选择具体流水线、该流水线的手动发布代码触发节点以及代码分支或 Tag，并由服务端原子校验、保存不可变执行快照，再按发布组依赖、组内串并行及失败策略推进。实际流程阶段、审核路径、部署环境和发布目标只能来自所选流水线的当前启用快照，前端不得临时改写、任取流水线或跳过无效应用。
- 发布计划列表必须按计划内应用展示各自最近一次关联流水线运行，并汇总当前状态：存在失败或阻塞时优先显示红色，存在正在执行、等待审核或待继续的运行时显示黄色，其余已有成功运行时显示绿色；状态使用克制的呼吸灯和明确文字共同表达，不能只依赖颜色。发布计划、流水线运行和发布记录必须通过 WebSocket 或低频轮询自动更新，页面可见或重新获得焦点时立即同步，不得要求用户手动刷新才能看到状态变化。
- 流水线运行卡片必须优先展示提交说明、短分支名、短 Commit、当前节点名称和简明状态；不得展示仓库 UUID、`refs/heads/...` 等冗长引用、轮询触发方式、流水线修订版号或内部过程文案。状态文案使用“当前节点”“状态”等直接表达，不生成总结式成功话术。
- 所有 Git 版本摘要必须明确显示引用类型并统一为 `tag: v1.0.2 · fea2410d`、`branch: main · fea2410d` 或 `pr: #12 · fea2410d`；不得使用含糊的 `git:` 或只展示裸引用名。类型判断必须使用完整 Ref 前缀，无法可靠判断时显示 `ref:`，禁止仅凭 `v1.0.2` 等名称猜测 Tag。
- 流水线运行页面必须提供脱离编辑器的只读执行拓扑，使用运行启动时保存的代码源、阶段和任务快照，突出当前任务并以克制的呼吸或流光动效表达正在执行；不得带入任务库、属性面板、拖拽、编辑或缩放等编辑器功能。缺少逐任务执行历史时只能准确标识当前任务，其余任务保持中性，禁止根据顺序猜测并伪造已完成状态。
- 流水线运行拓扑的节点必须使用完整且一致的卡片轮廓：节点类型通过图标及其浅色底表达，当前执行状态通过整圈边框、状态徽标和克制阴影表达；不得叠加会被端口截断或因状态切换而忽隐忽现的左侧装饰色条，也不得让类型色与状态色同时争夺卡片边框层级。
- 发布计划表示一次人工组织的批量发布；创建时用户只填写说明并在默认发布组内选择应用，不要求填写计划名称或版本，服务端生成内部标识且界面不展示。发布组必须创建在发布计划内部，用于编排多个应用，并明确组内并行或串行、组间依赖和失败策略。
- 发布计划、发布组和应用的唯一层级必须是“发布计划 → 发布组 → 多个应用”。发布计划接口、服务输入和界面不得再提供顶层应用字段或顶层应用选择步骤；计划的应用数量只能从各发布组成员汇总，新增、删除和排序应用必须明确发生在某个发布组内。
- 发布计划必须支持修改、删除、停用和重新启用；停用是独立于草稿、执行中、已完成等生命周期状态的可用性开关，不得伪装成取消历史执行。没有 `pending` 或 `running` 执行时允许删除计划，即使已经存在成功或失败的历史执行；删除必须使用 GORM 软删除，普通列表和详情隐藏计划，但发布计划、发布组、应用编排和历史执行记录必须继续保留用于审计。新计划使用服务端生成的默认发布组，常规界面不提供新增或删除发布组入口；默认组没有依赖时不得显示“无前置发布组”等无信息量文案。计划头部“编辑”用于整体配置，组内“添加应用”必须使用专用应用选择交互，不得复用同一个泛化编辑入口造成重复操作。应用项直接提供删除应用并明确不会删除应用本身或流水线配置；应用顺序通过拖拽保存。“组内顺序发布”默认关闭，关闭时组内应用并行执行，开启时严格按保存顺序逐个执行。
- 创建发布计划时必须至少选择一个应用，并允许一次选择多个应用；选择手动部署时，必须为每个应用明确指定 Git 分支或完整 Commit，不能依赖含糊的“当前版本”。
- `pipeline_runs` 表示一次由手动操作或代码事件触发的“流水线运行”，不得再把单次运行对外称为发布计划，以免与迭代级发布计划混淆。
- 代码仓库只管理 Git 来源、凭据和可选 Webhook，不绑定构建方案或部署方案；构建任务显式选择构建方案，部署任务显式选择部署方案，流水线运行必须保存启动时各任务实际使用的方案快照。
- 构建方案是可独立维护的资源，必须支持编辑、停用或重新启用以及删除；删除使用 GORM 软删除保留历史审计，仍被流水线引用时必须拒绝。列表至少展示启用状态、构建类型、Dockerfile 或脚本入口、构建上下文、产物路径、超时、说明和最近修改时间。Dockerfile 构建默认产生本地 OCI 镜像，可选推送到镜像仓库；脚本构建必须声明产物路径并生成可下载的文件制品。脚本构建和流水线 Shell 任务必须在隔离运行镜像中执行，默认镜像为 `alpine:3.22`，镜像必须提供 `/bin/sh`，只接受带明确非 `latest` Tag 或 Digest 的 OCI 镜像引用，并将镜像引用保存到运行快照。构建方案页面必须按“构建方案 → 方案概览 / 构建制品”的层级展示，制品不得与构建方案列表平级铺在同一页面；制品列表和手工上传必须固定当前 `build_plan_id`，应用只能作为方案内的归属和二级筛选条件。
- 部署方案也是独立可维护资源，必须支持编辑、停用、重新启用和 GORM 软删除。删除前必须在同一事务内检查所有应用流水线和公共流水线方案，草稿或停用配置中的引用同样必须阻止删除。停用方案不得通过新启用流水线的验证，已启动的运行继续使用启动时保存的不可变快照。
- Docker 容器部署方案只允许由 ZRT 按部署目标隔离创建的命名卷，不允许用户配置宿主机目录 bind 挂载。单容器部署网络固定使用 `bridge`；需要多服务网络互联时必须使用 Docker Compose 部署方案。
- Docker 容器和 Docker Compose 部署方案不得提供镜像地址输入；镜像只能来自流水线上游登记的不可变 OCI 制品。Compose 目标服务允许省略 `image`，执行时由服务端安全注入；旧 `${ZRT_IMAGE}` 占位符继续兼容，固定镜像或 `build` 必须拒绝。界面不得展示未实际执行限制的“运行时资源规格”；该概念属于构建或脚本任务临时运行容器的 CPU、内存配额，只有后端真实实施资源限制后才能提供配置。
- 新流水线使用版本化的“唯一代码源 + 串行阶段数组 + 阶段内串行任务数组”结构化定义，不允许自由连线，也不保存节点坐标或视口作为执行语义。本次重构不兼容旧画布、旧节点定义或旧流水线运行数据，不得为旧结构保留解析或执行兜底分支。阶段边界的“＋”按钮必须与任务主线共用同一纵向定位令牌，并以按钮中心对齐主线，禁止用独立的 `padding` 或 `top` 魔法数近似定位。
- 构建方案只负责把已固定 Commit 的源码生成可校验的不可变制品：Dockerfile 构建输出 OCI 镜像，脚本构建必须声明产物路径并由 ZRT 打包、计算摘要和登记制品。Dockerfile 路径、构建上下文、构建参数和产物路径不得出现在部署方案中；部署阶段只能消费已登记的制品，不得重新检出或构建源码。
- 一个应用只能关联一个代码仓库，不提供应用内多仓库、仓库排序、顺序发布或无序发布配置；多个应用之间的并行、串行、依赖和失败策略只能在发布计划的发布组中编排。
- 应用名同时作为 Dockerfile 制品的镜像仓库名，新建和修改时必须以小写英文字母开头，仅允许小写英文字母和用于分隔单词的单个下划线。构建方案不得再配置独立仓库名；选择镜像仓库后，界面必须预览“仓库地址/命名空间/[应用名]:[版本标签]”，实际版本标签只使用 12 位提交短哈希。重复构建同一 Commit 可以更新同名展示标签，但制品登记、部署和回滚必须始终使用构建后固定的完整 Digest，禁止把短标签当作不可变身份。
- 环境、主机和发布目标继续作为独立的内部资源：环境只作为用户自定义的基础设施分组，不设置开发、测试/预发布、生产等安全级别；主机保存 SSH 接入和 SSH 命令部署、Docker、Kubernetes 多选能力；内部 `DeploymentTarget` 保存主机或运行时、工作目录、命名空间和容器等执行参数。面向用户时，“怎么执行”和“部署到哪里”必须组成同一个部署方案，通过一个表单和一次事务保存；流水线部署节点不得再单独选择发布目标，发布目标必须由所选部署方案唯一解析。编辑部署方案时切换部署方式不得销毁尚未保存的输入；主机脚本、Docker 容器、Docker Compose 和 Kubernetes Deployment 必须分别维护当前表单会话中的独立草稿，即使字段名称相同也不得在不同方式间自动复用或覆盖。
- 一个环境可以选择多台主机，一台主机也可以由用户显式加入多个环境；环境归属只表示基础设施逻辑分组，不得以“主机已属于其他环境”为由禁选、迁移或解除既有关联。部署方案只选择环境，不再重复展示或接受执行主机输入；服务端必须根据部署方式从环境内已就绪能力解析唯一执行主机并保存到内部目标快照，没有匹配主机或同一种能力存在多台候选主机时拒绝保存并提示先调整环境，禁止静默选择第一台。Kubernetes 集群仍由用户明确选择，关联执行主机同样由环境和集群能力唯一解析。服务端口、容器名称、工作目录及 Kubernetes 命名空间/工作负载等冲突必须按具体部署方式在配置或真实执行边界处理，不能使用环境归属关系推断冲突；尚未提供结构化服务端口配置时，界面不得声称已经完成端口预检。流水线部署任务只选择一个已启用的部署方案，环境、主机、运行时、工作目录、容器名称、命名空间和工作负载都由该方案唯一解析，任务不得再保存或展示独立 `environment_id`、发布目标或“部署到哪里”步骤。服务端必须校验部署方案及其内部目标的类型和归属关系，流水线运行保存部署方案与内部目标的不可变快照。
- 部署方案表单必须先选择环境，再按该环境关联主机的已就绪能力展示可用执行方式；Docker 容器和 Docker Compose 不向用户暴露或要求选择执行主机及内部 Docker 连接。Docker 容器名称为可选项，留空时在流水线运行快照中生成“应用名-短隔离标识”的稳定名称；隔离标识必须绑定应用、部署方案和内部目标，保证同一应用可以通过不同方案重复部署到同一主机且后续更新仍命中原容器。Docker 容器的自定义部署命令在目标主机执行，用于完整替代 ZRT 内置容器参数和创建命令，绝不能写入容器 `ENTRYPOINT`/`CMD`；只允许经过语法解析的单条 `docker run`，镜像和容器名使用受控占位符，禁止 Shell 串联、特权模式、主机网络、宿主机目录和 Docker Socket 挂载。环境变量、命名卷、自定义部署命令和自定义健康检查统一收纳到“高级配置”。
- 新建远程主机通过表单配置 SSH 地址、端口、用户名，以及密码或私钥，并多选 SSH 命令部署、Docker、Kubernetes 能力；保存前必须真实测试 SSH 和所有勾选能力，测试结果必须由服务端短期令牌绑定本次输入，不能仅信任客户端回传的指纹。SSH 密码或私钥必须加密保存，测试成功后固定 SHA256 主机公钥指纹。不恢复宿主机网页登录、任意命令终端或文件管理。
- 编辑远程主机时默认由服务端复用已加密保存的 SSH 凭据，不要求用户重复输入密码或私钥；地址、端口、用户名或能力改变后仍必须使用已保存凭据完成真实重测。只有用户明确选择替换凭据时才显示并要求新密码或私钥，且新凭据必须在保存前测试成功。
- SSH 命令部署只能执行已保存并随流水线快照的脚本部署方案，发布目标只定义执行主机、工作目录和超时，不在发布目标中重复保存命令。执行必须非交互、固定使用受控 shell 入口，校验并固定主机指纹，限制脚本体积和超时，完整归档 stdout/stderr、退出状态和审计上下文；不提供临时命令 API，默认不自动重试，也不伪造自动回滚。
- Kubernetes 集群始终是独立的一等资源并通过 kubeconfig、集群内身份或 Kubernetes API 接入；主机勾选 Kubernetes 只表示节点或管理能力，可关联已有集群，但不得把一台主机等同为整个集群，也不得绕过 kubeconfig 改为任意 SSH `kubectl` 执行。托管集群可以没有可 SSH 主机。
- 远程 Docker SSH 连接必须支持普通用户直接执行 Docker 和 `sudo` 两种方式；密码认证启用 sudo 时默认复用 SSH 登录密码，不得要求用户重复填写，私钥认证则要求远程用户配置免密 sudo。后端每次建立执行通道时先自动检测免密 sudo，需要密码时才通过 SSH 标准输入传递已有 SSH 密码；密码不得拼接到命令、日志、审计元数据或接口返回中。连接测试、Docker API 隧道、镜像传输、部署和容器管理必须统一使用同一项 sudo 配置。
- 环境和发布目标不得携带或推导安全级别，制品校验必须由发布方式决定，不能因环境名称或分类而放宽。是否审核、审核位置及后续任务完全由流水线阶段中用户明确配置的审核任务决定；没有审核任务的部署路径必须允许执行，明确配置的审核任务仍须执行权限、自审限制和审计。
- 审核任务通过后必须由后端立即推进到同阶段下一任务或下一阶段；下一任务是部署时直接创建执行任务，只有明确配置的人工放行任务才能再次暂停。前端不得要求用户在审核通过后点击“继续流水线”。
- 流水线进入构建任务时必须创建受审计的 NATS 执行任务，检出已固定 Commit，执行 Dockerfile 或非交互脚本，并登记内容摘要可验证的不可变制品。部署任务只能消费上游制品和已快照的部署方案，不得再次检出代码或隐式构建；只有真实部署成功后才能把流水线标记为成功，禁止只推进任务状态或返回模拟成功。
- 每次真实流水线执行必须按阶段持久化检出、构建、传输、部署和最终结果日志，并保存 Docker/BuildKit 原始构建输出；运行中通过带登录会话和 `delivery.read` 权限校验的 WebSocket 实时查看，结束后仍可读取历史日志。日志必须限制单条和单次运行的最大体积，不得写入平台令牌、密码或解密后的凭据。
- 失败的流水线运行允许重新执行，但不得复活、覆盖或删除原失败记录；重新执行必须使用相同 Git Ref 和 Commit、新建流水线运行和 NATS 任务，并记录来源运行 ID。构建方案、部署方案和流水线配置使用重新执行时的当前有效版本；明确配置的审核任务必须在新运行中重新完成，不得复用旧审核结果。
- Dockerfile 构建默认只复用当前 Docker/BuildKit 运行时的本地层缓存；Compose 的缓存位于 Docker-in-Docker 持久卷，本地二进制复用宿主机 Docker 缓存。禁止为缓存额外构建、标记、拉取或推送 `zrt-cache` 等镜像，镜像仓库只保存正式制品版本；关闭构建缓存时使用 BuildKit 的 `--no-cache`。Redis 不保存源码、镜像层或大体积构建产物。

## 代码风格规范

- 新增和修改代码必须贴合所在模块既有的命名、分层、错误处理和测试习惯，避免出现与项目风格割裂的模板化“AI 代码”。
- 只实现需求所需的最小清晰结构，不为展示完整性增加无实际调用的接口、空泛扩展点、重复包装层、过度通用的辅助类型或不必要的设计模式。
- 注释重点说明设计原因、业务约束和容易误用的边界，不逐行复述代码，不添加“初始化”“处理数据”“返回结果”等无信息量注释，也不使用装饰性分区注释堆砌文件结构。
- 命名和控制流应自然、直接、符合语言惯例；能在局部清楚表达的逻辑不随意拆成大量单次调用函数，已有成熟包或项目公共能力可以解决的问题不得重新实现一套近似版本。
- 测试围绕真实行为、失败边界和回归风险编写，不生成机械重复、只验证常量或只为提高数量的展示性用例。
- 对外文案、日志和文档使用简洁自然的中文，避免空泛总结、营销式措辞和明显的机器生成套话。

## 前端交互规范

- 名称、地址、端口、认证方式等结构固定的用户资源必须使用带校验和明确字段说明的表单创建、编辑，不得把手写 JSON 作为主交互；只有确实需要保留任意结构的高级配置才允许提供 JSON 编辑器。
- 所有单选下拉框使用一致的内收箭头，箭头与控件右边缘保留舒适安全距离，同时为选项文字预留足够右侧内边距；浅色和深色主题下都必须清晰可见。多选框不得套用单选下拉箭头。
- 长表单抽屉必须让正文区域独立滚动，并使用抽屉原生 footer 承载固定操作按钮；不得在正文滚动容器内使用带负边距的 sticky 操作栏，以免遮挡末尾字段或导致滚动到底仍无法完整查看内容。常规高度和低高度窗口下都必须能滚动到最后一个字段及说明。
- 保存、启用、删除等会发起异步请求的确认弹窗必须使用受控提交状态：点击确认后立即显示加载并阻止重复提交，成功后明确关闭，失败时保留上下文并显示可操作的错误提示；不得让用户面对无加载反馈、看起来“点不动”的确认按钮。
- 表单中用于选择可创建关联资源的下拉框，必须在控件右侧提供可访问的“＋”创建入口；仅在用户拥有对应创建权限时显示，点击后应跳转并直接展开对应资源的创建界面，或聚焦到不会自动写入数据的明确创建入口，不得要求用户退出当前流程后再从侧栏查找。
- Git 令牌必须按用户隔离并加密保存，任何账户（包括超级管理员）都不得通过用户接口查看其他用户的令牌明文；仓库只能引用当前操作者自己的已保存令牌。
- Webhook 签名密钥允许拥有 `repository.secret.read` 权限的用户重复查看，不再采用只显示一次的交互；每次读取必须经过鉴权并写入审计日志。
- RBAC 同时支持角色权限和用户级权限覆盖；用户级显式拒绝优先于角色授权，用户级允许用于少量例外，前端菜单可见性与后端接口必须使用同一份有效权限结果。
- 新建镜像仓库表单必须提供“测试”按钮；测试应使用尚未保存的配置执行真实 OCI Registry 登录，只有登录成功才显示测试成功，且测试过程不得持久化仓库配置或明文凭据。
- 镜像仓库类型与地址语义必须一致：Docker Hub 使用系统固定的 `docker.io`，表单不得要求或允许用户填写地址；Harbor 和通用 OCI Registry 必须显式填写地址，阿里云 ACR 等厂商仓库归入通用 Registry。服务端必须按类型规范化并校验，禁止保存 `docker_hub` 类型配第三方仓库地址的矛盾配置。
- RBAC 授权判定统一使用 Casbin；现有 GORM 角色、权限和用户覆盖表是唯一事务数据源，通过只读策略适配器加载到 Casbin，并使用 Redis Watcher 同步多实例策略，禁止再维护另一套自研权限匹配逻辑或重复的 `casbin_rule` 影子数据。
- 登录失败锁定必须由拥有配置管理权限的用户在“系统设置”中显式开启，默认关闭；开启后才使用 Redis 失败计数和有限锁定窗口，开关变化时清理已有失败计数，不影响已经建立的登录会话。
- LDAP、OAuth 等登录方式统一放在“系统设置”的登录方式分区，不再提供独立侧栏入口；页面可见性和读写操作继续分别使用 `identity.read`、`identity.manage`，不得因合并入口而要求无关的配置权限。
- 登录后 Header 左侧统一显示路径面包屑，不再显示 `ZRT` 产品眉题或页面大标题；面包屑参考 Vben Admin 的路由层级表达，按“功能域 > 菜单分组 > 当前页面”生成，父级可点击跳转、当前页不可点击，并与基于历史访问的标签栏保持不同语义。面包屑左侧提供按真实访问顺序工作的返回按钮和最近页面下拉，保留同一路由不同参数的访问记录，并在当前浏览器会话刷新后继续可用。Header 下方显示已访问页面标签栏；访问页面后保留可切换标签，当前标签必须使用浅色底与底部主色指示线突出显示，概览标签固定不可关闭，其他标签允许关闭，关闭当前标签时切换到左侧最近的已打开页面。标签右键菜单必须提供关闭、固定或取消固定、最大化、重新加载、新窗口打开、关闭左侧、关闭右侧、关闭其他和关闭全部，批量关闭必须保留固定标签。页面向下滚动时，带面包屑的 Header 与历史标签必须分层吸附，标签栏不得滑入 Header 下方；Header、标签栏和内容区按 Vben Admin 使用克制的细边框分层，不再保留旧版悬浮标签造型。
- 登录后整体布局固定为侧栏、含面包屑的 Header、历史标签和页面内容四个独立视觉面；侧栏、Header 与标签栏使用白色或深色主题表面，内容区使用对应的浅灰或深灰布局底，边界以 1px 细线分隔，卡片再使用统一圆角和轻阴影。
- 登录后页面不得在 Header 或内容区重复显示产品眉题、页面名称或同义的大标题。内容区顶部统一使用紧凑操作栏承载页面说明、状态和主要操作；只有表单、弹层或能明确区分当前内容的子分区才保留独立标题。
- 应用卡片使用限制最大宽度的响应式网格，单个应用不得横向撑满超宽内容区；代码仓库和流水线列表必须按绑定 ID 可靠解析并作为可点击入口，不得因列表接口未预加载关联对象而误显示“未绑定”。构建方案和部署方案只在对应流水线任务中选择，不得回填到应用卡片或应用表单。应用卡片只提供应用配置、流水线配置和代码更新检查，不显示手动执行入口；手动执行统一从“流水线运行”页面发起，并依次选择应用、流水线和代码版本。
- 应用表单和应用卡片不配置或展示审核开关；审核是否发生仅由流水线阶段中的审核任务决定，没有审核任务的部署路径不得被隐式要求审核。
- 整体界面统一采用 Vben Admin 5 `web-antd` 的设计语言：固定侧栏、紧凑 Header、历史标签、浅灰内容底、白色卡片、Ant Design Vue 表单与表格以及克制的路由切换动效必须覆盖全部页面，不得只仿照单个看板。正文和状态文字不得因追求信息密度而过小；必须遵循 `prefers-reduced-motion`，不使用干扰阅读或拖慢操作的装饰动画。
- 登录后整体布局采用紧凑但有呼吸感的统一密度，侧栏、Header、历史标签、页面边距、操作栏、卡片、表格和表单应减少无效留白；不得为了紧凑缩小正文到难以阅读、压缩必要点击区域、破坏对齐或移除体现层级的圆角、阴影和留白。窄屏侧栏应限制首屏占用并允许内部滚动，不能把完整导航无限纵向铺开。
- 登录后的右侧主工作区按 Vben Admin 5 使用铺满剩余视口的浅灰内容底；Header 与历史标签固定为独立白色层，页面卡片再使用圆角、细边框和轻阴影表达层级，不额外套一层悬浮式总面板。
- 前端必须提供全局浅色和深色主题，切换入口固定在右上角并覆盖登录页、业务页面、表单、弹层和流水线编辑器；用户选择保存在浏览器本地，首次访问跟随系统主题，首屏加载不得先闪烁为另一种主题。新增界面必须使用主题令牌，不得继续写死仅适用于白色背景的颜色。
- 默认语言为简体中文，并通过 Vue I18n 与 Ant Design Vue Locale 支持 English；应用外壳、导航和新增公共文案必须使用国际化键，不得在布局组件中继续散落不可切换的固定语言。
- 流水线编辑器采用类似 Gitee 的固定图形结构：左侧固定唯一代码源，右侧以分栏方式横向排列阶段；串行关系使用从左到右的主线表达，并行关系必须从主线向下分叉、以纵向泳道排列并在末端汇合，禁止把并行任务和“新的任务”横向接在同一条线上造成串行错觉。空阶段显示明确的虚线“新的任务”入口；阶段已有任务时，选中该阶段中的任务才显示带 U 形分叉线的“新建并行任务”入口，未选中时隐藏，点击后打开按构建、测试、扫描、发布、部署、工具分类的任务库。打开任务库或点击关闭按钮时必须保留触发入口的任务选中态和分叉线；点击画布空白或任务库右上角的删除图标只取消尚未落库的“新建并行任务”入口并清空选中态，不得删除任何已有任务。只有从任务库点击具体任务后才真正新增节点，新增后的任务不得因后续点击空白而被删除。任务上下拖动只调整并行分支排列。点击阶段或任务后从右侧抽屉编辑；不得提供自由连线、节点坐标、缩放或无限画布。并行分支的最终执行能力必须具有真实并行语义，不能只改变视觉布局。未启用草稿需防抖自动保存并显示保存状态，已启用流水线的修改必须经用户明确确认，不得因自动保存而停用或改写正在执行的规则。
- 流水线编辑器顶部只保留可直接修改的方案名称，并与“新建方案”、检查和保存操作放在同一紧凑操作栏；不得另起卡片重复展示方案说明或修订号。右侧任务库和属性面板必须限制在编辑区可视高度内并独立滚动，不得越过页面滚动边界；打开和关闭使用克制的侧滑淡入动画，并遵循 `prefers-reduced-motion`。
- 侧栏只保留“主机与集群”入口，不再单列“容器与集群”。同一页面内分为“主机与接入”和“运行资源”：前者维护 SSH、环境归属和 SSH 命令部署、Docker、Kubernetes 多选能力，后者按“主机 → Docker/Kubernetes 运行时”浏览容器、Pod、日志和终端；未关联主机的历史运行时必须明确标记，不能静默丢失。
- “主机与接入”的主操作区必须同时提供“添加主机”和“添加 K8s 集群”，不得把创建独立 Kubernetes 集群隐藏在主机表单内部；只有拥有 `cluster.manage` 权限时显示。新集群配置必须先使用未保存的 kubeconfig 或集群内身份真实测试 Kubernetes API，测试成功后才能创建，创建成功后立即同步到运行资源列表。
- “主机与集群”的运行时状态使用轻量状态接口每 5 秒自动同步：页面隐藏时必须停止轮询，重新可见或获得焦点时立即同步，并对同时触发的可见性与焦点事件去重；轮询不得重复加载完整主机配置和环境归属。最近一次真实连接成功使用绿色，连接失败使用红色，尚未关联或首轮检查尚未完成使用黄色；`runtime_id` 只表示关联关系，不能单独作为绿色健康状态的依据。页面不得要求用户手动刷新状态。
- “环境管理”“主机与集群”的主机与接入视图和“部署方案”使用保留式主从布局：左侧资源列表在查看详情、创建和编辑时始终保留，右侧只展示当前资源详情或表单；Docker、Docker Compose 与 Kubernetes Deployment 必须显示对应运行时品牌图标，点击列表项先进入详情，再通过明确的编辑操作修改。环境详情顶部的“编辑信息”只修改名称和说明，“所属主机”分区的“调整主机”只修改主机归属，两个入口不得复用同一套完整表单造成重复功能。
- 主机和 Kubernetes 集群属于用户资源，创建后必须允许拥有管理权限的用户编辑并持久化；远程主机地址、端口、用户名、凭据或能力发生变化时必须重新执行真实 SSH 与相应能力测试，只有测试成功才能保存。本地 Docker 运行时由 ZRT 启动方式决定，只允许修改显示名称，不得伪装成可编辑的 SSH 主机。Docker 和 Kubernetes 的运行时标识使用各自标准品牌图标，不得用字母占位。
- 主侧栏必须使用最多两级的可折叠树组织功能域，当前页面所属分支自动展开，并按有效权限隐藏无权访问的叶子节点；视觉层级参考 Vben Admin：主菜单使用清晰图标和舒适行高，当前功能域的父级使用蓝色文字与图标强调，当前叶子使用中性浅灰背景，子菜单直接缩进且不绘制树状连接线。桌面端侧栏与视口左侧贴合并保持全高，可向左收起为图标栏、向右恢复完整菜单并按用户保存状态；侧栏和主工作区之间使用细边框分隔，不使用悬浮外边距。任务、监控、通知、调度、配置、发布计划、流水线运行、发布记录、用户、角色和审计等不同资源必须有独立入口和独立页面状态，不得再集中堆放在一个菜单页签中。
- “可观测与运维”主菜单只保留“系统监控”、“任务中心”和“日志”三个高频入口；HTTP 监控、通知渠道/记录、定时任务和原始配置中心不在主侧栏展示，未明确要求时不删除已有 API 和历史数据。“日志”独立展示当前 ZRT 进程及内部模块输出的系统运行日志，与流水线执行日志、任务输出和审计日志分开；不得采集或汇总部署容器的 stdout/stderr，容器详情已有的只读控制台日志只允许用户打开时按需读取。
- “系统设置”对所有已登录用户提供修改自己密码的入口，修改成功必须使所有旧会话失效并要求重新登录。具有配置权限的用户可设置流水线日志和审计日志的独立保留天数、开启自动清理或明确手动清理；新安装默认不自动删除历史日志。


<claude-mem-context>
# Memory Context

# claude-mem status

This project has no memory yet. The current session will seed it; subsequent sessions will receive auto-injected context for relevant past work.

Memory injection starts on your second session in a project.

`/learn-codebase` is available if the user wants to front-load the entire repo into memory in a single pass (~5 minutes on a typical repo, optional). Otherwise memory builds passively as work happens.

Live activity: http://localhost:37701
How it works: `/how-it-works`

This message disappears once the first observation lands.
</claude-mem-context>
